<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.school.forum</groupId>
    <artifactId>school-forum</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>school-forum</name>
    <description>校园论坛系统 —— 单体多模块架构</description>

    <!--
      模块依赖方向（严禁出现反向依赖或环，由 ArchUnit 单元测试强制校验）：

        forum-common  ←  forum-infrastructure  ←  { forum-user, forum-forum,
                                                    forum-notification, forum-seckill }
                                                   ↓
                                              forum-admin  →  forum-boot（唯一可执行模块）

      业务模块之间只允许通过对方 api 包下的接口调用，不得直接引用对方的
      service / mapper / entity。这是未来拆分微服务的唯一前提。
    -->
    <modules>
        <module>forum-common</module>
        <module>forum-infrastructure</module>
        <module>forum-user</module>
        <module>forum-forum</module>
        <module>forum-notification</module>
        <module>forum-seckill</module>
        <module>forum-points</module>
        <module>forum-admin</module>
        <module>forum-boot</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <revision>1.0.0-SNAPSHOT</revision>

        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <redisson.version>4.7.0</redisson.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.8.5</springdoc.version>
        <archunit.version>1.4.0</archunit.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ========== 内部模块 ========== -->
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-common</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-infrastructure</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-user</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-forum</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-notification</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-seckill</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-points</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.school.forum</groupId>
                <artifactId>forum-admin</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- ========== 第三方 ========== -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!--
              MyBatis-Plus 3.5.9 起把依赖 JSqlParser 的插件拆到了独立 artifact，
              分页插件 PaginationInnerInterceptor、防全表更新插件 BlockAttackInnerInterceptor
              都在这里面。不引这个包，编译期会报「找不到符号」。
              （不依赖 JSqlParser 的插件如 OptimisticLockerInnerInterceptor 仍在主包里。）
            -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 所有模块共用的基础依赖 -->
    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <!--
                          annotationProcessorPaths 必须显式声明，否则 Lombok 与 MapStruct
                          同时存在时注解处理器顺序不确定，会导致 MapStruct 生成的实现类
                          读不到 Lombok 生成的 getter/setter。
                          顺序固定为：lombok → lombok-mapstruct-binding → mapstruct-processor
                        -->
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>0.2.0</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
