/**
 * 后台管理：内容审核、用户管理、活动管理，以及 MQ 选型压测结果的查询与导出。
 *
 * <p><b>「选型压测结果查询」为什么放在后台模块：</b>
 * 本项目要把同一套业务逻辑分别跑在 Kafka 和 RabbitMQ 上，
 * 用数据决定最终保留哪一个。压测结果落在
 * {@code t_mq_benchmark_result}，消费明细落在 {@code t_mq_consume_log}，
 * 需要有人能方便地看这批数据。做成后台页面比每次手写 SQL 更实用，
 * 也顺便让「对比结论」成为可复现、可追溯的东西，而不是一次性的临时表格。
 *
 * <p><b>本模块只允许依赖其他业务模块的 {@code api} 包。</b>
 * 后台天然有「什么数据都想直接查」的倾向，如果放任它直接访问
 * 各个模块的 mapper，其他模块的内部表结构就会被外部锁死——
 * 到时候想给帖子表加个字段都得先确认后台有没有在用它。
 * ArchUnit 测试会拦住这类越界调用。
 *
 * <p>包结构与模块约定见 {@code com.school.forum.user} 的 package-info。
 */
package com.school.forum.admin;
